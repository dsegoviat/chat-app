# Triage labels

These labels map the canonical triage roles used by agent skills to the exact label strings for this repository.

## Canonical mapping

- `needs-triage` -> `needs-triage`
- `needs-info` -> `needs-info`
- `ready-for-agent` -> `ready-for-agent`
- `ready-for-human` -> `ready-for-human`
- `wontfix` -> `wontfix`

## Usage rule

When triaging, skills should apply and transition using the mapped label strings above to avoid creating duplicate or unintended labels.
